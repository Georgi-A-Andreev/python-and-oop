-- PROBLEM 1

SELECT title
FROM books
WHERE title LIKE 'The%'
ORDER BY id

-- PROBLEM 2

SELECT REPLACE(title, 'The', '***') AS title
FROM books
WHERE title LIKE 'The%'
ORDER BY id

-- problem 3

SELECT id,
		(side * height) / 2 AS area
FROM triangles
ORDER BY id

-- problem 4